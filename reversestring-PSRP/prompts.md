You are an expert web developer working with HTML and JavaScript.
The folder eversestring-PSRP is having a skeleton for an HTML page that we need to modify.
I want you to modify eversestring-PSRP/index.html and eversestring-PSRP/script.js so we finally have in the the page five sections distributed in a vertical layout
Sections to be included in the page:
1- Static title text "Reverse String"
2- Input text full width 
3- Reverse button with text "Reverse 🔄" with blue background
4- Output text section => It must be a parragraph
5- Button with text "Copy 🗒️" with dark grey background

You shoulld also implement a couple of functions in the script.js file 
1- one function to reverse a string so the function accepts a string and it returns the reversed value for example: AI4Devs returns sveD4IA
2- one function to copy the value from the Output text section to the clipboard

You must also implement a mechanism so every time the reverse button is clicked we call the reverse function from the script and place the result into the output text
